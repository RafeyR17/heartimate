/** WCAG 2.5.5 minimum touch target (px). */
export const TOUCH_TARGET_MIN_PX = 44

/** Minimum 44×44px touch target for icon buttons (WCAG 2.5.5). */
export const iconTouchClass =
  'inline-flex items-center justify-center min-h-[44px] min-w-[44px] p-2.5 rounded-lg'

/** Primary CTA links/buttons (e.g. Enter Free). */
export const primaryCtaTouchClass =
  'min-h-[44px] inline-flex items-center justify-center'
